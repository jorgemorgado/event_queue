#!/usr/bin/q
#
# This script is provided as an example only.
#
# Author: Jorge Morgado <jorge (at) morgado (dot) ch>
#

array = [ 1, 2, 3, 3, 4, 5]

print "Have %d items in array" % len(array)

for item in array:
    print item

print "Done!"
